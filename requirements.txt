# Runtime dependencies. Single source of truth — keep in sync with pyproject.toml.
aiogram>=3.0.0,<4.0.0
SQLAlchemy>=2.0.23,<3.0.0
asyncpg>=0.27.0
# psycopg2-binary is required by `src/database/migrations/env.py` for sync
# Alembic migrations. The bot itself uses asyncpg at runtime; do NOT drop
# psycopg2-binary unless env.py is also refactored to async (see BUG-004).
psycopg2-binary>=2.9.0
alembic>=1.13.1
openai>=1.0.0
emoji>=2.2.0
numpy>=1.26.0,<2.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
python-dateutil>=2.8.2
pytz>=2023.3
greenlet>=3.1.1
